node_modules
dist
.DS_Store
# build-time bundles produced by scripts/gen-*.mjs from committed data/ sources
# (regenerated by `npm run gen`, which the Pages build runs before vite build)
src/**/*.generated.json
